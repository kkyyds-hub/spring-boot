<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.studysystem.mapper.CategoryMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into material_category(user_id, name, sort, create_time)
        values (#{userId}, #{name}, #{sort}, #{createTime})
    </insert>

    <update id="update">
        update material_category
        set name = #{name},
            sort = #{sort}
        where id = #{id}
    </update>

    <delete id="deleteById">
        delete from material_category where id = #{id}
    </delete>

    <select id="list" resultType="MaterialCategory">
        select id, user_id, name, sort, create_time
        from material_category
        order by sort, id
    </select>
</mapper>
